# Copy to .env.production before `npm run build`
#
# Same server (nginx proxies /api): leave empty
# VITE_API_URL=
#
# Split hosting (frontend on CDN, API elsewhere):
# VITE_API_URL=https://api.your-domain.com
